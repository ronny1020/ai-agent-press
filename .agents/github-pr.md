# github-pr

A sample skill file named `github-pr` used by tests. This file ensures the sidebar and headless listing include an item with the slug `github-pr`.

- name: github-pr
- description: Example skill for tests
